# Datenbank-Verbindung
# Lokal (Standard): SQLite - kein Setup nötig, wird automatisch erstellt.
# Docker Compose / Produktion: PostgreSQL-URL setzen.
# DATABASE_URL=postgresql+psycopg://todo:todo@localhost:5432/todo
